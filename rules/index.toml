version = "1.0.0"
total_rules = 263
last_updated = "2026-03-17"

[categories.logical]
description = "Logical transformations that preserve query semantics"

[categories.logical.predicate-pushdown]
description = "Push selection predicates closer to data sources"
rules = [
    "filter-through-join",
    "filter-through-project",
    "filter-through-union",
    "filter-into-join-condition",
    "filter-merge",
]

[categories.logical.join-reordering]
description = "Reorder joins for optimal execution"
rules = [
    "join-associativity",
    "join-commutativity",
    "left-deep-to-bushy",
    "cartesian-to-join",
    "outer-join-to-inner",
]

[categories.logical.join-elimination]
description = "Eliminate redundant joins"
rules = []

[categories.logical.subquery-unnesting]
description = "Convert subqueries to joins"
rules = []

[categories.logical.projection-pushdown]
description = "Push projections to reduce data movement"
rules = [
    "project-merge",
    "project-through-join",
    "column-pruning",
]

[categories.logical.aggregate-pushdown]
description = "Push aggregations for early reduction"
rules = []

[categories.logical.expression-simplification]
description = "Simplify expressions and constants"
rules = [
    "constant-folding",
    "boolean-simplification",
    "arithmetic-simplification",
    "null-propagation",
    "common-subexpression-elimination",
]

[categories.logical.limit-pushdown]
description = "Push limits to reduce processing"
rules = []

[categories.logical.set-operations]
description = "Optimize UNION, INTERSECT, EXCEPT"
rules = [
    "union-merge",
    "intersect-to-join",
]

[categories.physical]
description = "Physical optimizations for execution"

[categories.physical.join-algorithms]
description = "Choose optimal join algorithms"
rules = []

[categories.physical.index-selection]
description = "Select appropriate indexes"
rules = []

[categories.physical.aggregation-strategies]
description = "Choose aggregation strategies"
rules = []

[categories.physical.parallelization]
description = "Parallel execution strategies"
rules = []

[categories.physical.materialization]
description = "Materialization strategies"
rules = []

[categories.database-specific]
description = "Database-specific optimizations"

[categories.database-specific.datafusion]
description = "Apache DataFusion Arrow-native query engine optimizations"
rules = [
    "datafusion-filter-pushdown-through-join",
    "datafusion-projection-pushdown",
    "datafusion-type-coercion",
    "datafusion-common-subexpr-elimination",
    "datafusion-scalar-subquery-to-join",
    "datafusion-limit-pushdown",
    "datafusion-aggregate-statistics",
    "datafusion-join-reordering",
    "datafusion-parquet-predicate-pushdown",
    "datafusion-hash-join-selection",
    "datafusion-repartition-insertion",
    "datafusion-eliminate-unnecessary-sort",
    "datafusion-simplify-expressions",
    "datafusion-propagate-empty-relation",
    "datafusion-single-distinct-to-groupby",
    "datafusion-decorrelate-subquery",
    "datafusion-window-function-optimization",
    "datafusion-replace-distinct-aggregate",
    "datafusion-unnest-rewrite",
    "datafusion-pipeline-breaking-optimization",
]

[categories.database-specific.materialize]
description = "Materialize streaming SQL / differential dataflow optimizations"
rules = [
    "materialize-temporal-filter-pushdown",
    "materialize-demand-projection",
    "materialize-join-implementation",
    "materialize-reduce-elision",
    "materialize-nonnull-requirements",
    "materialize-fusion",
    "materialize-arrangement-sharing",
    "materialize-reduce-reduction",
    "materialize-threshold-elision",
    "materialize-let-motion",
    "materialize-canonicalize-mfp",
    "materialize-topk-monotonic",
    "materialize-literal-lifting",
    "materialize-predicate-pushdown-through-join",
    "materialize-column-knowledge",
]

[categories.database-specific.oracle]
description = "Oracle Database cost-based optimizer transformations"
rules = [
    "oracle-star-transformation",
    "oracle-bloom-filter-join",
    "oracle-result-cache",
    "oracle-partition-pruning",
    "oracle-or-expansion",
    "oracle-subquery-unnesting",
    "oracle-join-predicate-pushdown",
    "oracle-view-merging",
    "oracle-index-fast-full-scan",
    "oracle-materialized-view-rewrite",
    "oracle-parallel-execution",
    "oracle-adaptive-plans",
    "oracle-hash-group-by",
    "oracle-table-expansion",
    "oracle-predicate-move-around",
    "oracle-in-memory-scan",
    "oracle-join-elimination",
    "oracle-connect-by-optimization",
    "oracle-batch-table-access",
    "oracle-group-by-placement",
]

[categories.database-specific.mssql]
description = "Microsoft SQL Server optimizer and execution engine optimizations"
rules = [
    "mssql-columnstore-scan",
    "mssql-adaptive-join",
    "mssql-batch-mode-execution",
    "mssql-parameter-sniffing",
    "mssql-indexed-view-matching",
    "mssql-batch-mode-on-rowstore",
    "mssql-partition-elimination",
    "mssql-spool-optimization",
    "mssql-memory-grant-feedback",
    "mssql-predicate-pushdown-computed",
    "mssql-hash-match-aggregate",
    "mssql-query-store-forced-plan",
    "mssql-star-join-optimization",
    "mssql-key-lookup-elimination",
    "mssql-window-aggregate-pushdown",
    "mssql-seek-predicate-optimization",
    "mssql-subquery-to-apply",
    "mssql-interleaved-execution",
    "mssql-in-memory-oltp",
    "mssql-trivial-plan",
]

[categories.execution-models]
description = "Execution model specific optimizations"
rules = []

[categories.cost-models]
description = "Cost estimation rules"
rules = []

[categories.hardware]
description = "Hardware-specific acceleration rules (GPU, FPGA, SIMD, NUMA)"

[categories.hardware.gpu]
description = "GPU-accelerated operators using CUDA/OpenCL"
rules = [
    "gpu-parallel-scan",
    "gpu-hash-join",
    "gpu-aggregation",
    "gpu-sort",
    "gpu-predicate-evaluation",
    "gpu-string-operations",
    "gpu-window-function",
    "gpu-distinct-aggregation",
]

[categories.hardware.fpga]
description = "FPGA streaming and near-storage acceleration"
rules = [
    "fpga-stream-filter",
    "fpga-compression-scan",
    "fpga-hash-join",
    "fpga-regex-filter",
]

[categories.hardware.accelerator]
description = "CPU-level hardware optimizations (SIMD, NUMA, cache)"
rules = [
    "heterogeneous-operator-placement",
    "simd-vectorized-scan",
    "numa-aware-partitioning",
    "prefetch-aware-join",
    "cache-conscious-partitioning",
]

[categories.hardware.data-placement]
description = "Host-device data transfer and memory management"
rules = [
    "host-to-device-transfer",
    "device-memory-caching",
    "columnar-conversion",
    "unified-memory-management",
]

[categories.distributed]
description = "Distributed query optimization rules for multi-node execution"

[categories.distributed.exchange-placement]
description = "Exchange operator insertion, merging, and removal"
rules = [
    "insert-exchange",
    "merge-adjacent-exchanges",
    "remove-redundant-exchange",
    "choose-exchange-type",
]

[categories.distributed.data-movement]
description = "Minimize data movement across the network"
rules = [
    "push-filter-below-exchange",
    "push-project-below-exchange",
    "push-limit-below-exchange",
    "minimize-network-transfer",
    "union-all-pushdown",
]

[categories.distributed.distributed-joins]
description = "Distributed join strategies (broadcast, shuffle, co-located)"
rules = [
    "broadcast-join",
    "shuffle-join",
    "colocated-join",
    "lookup-join",
    "semi-join-reduction",
    "skew-aware-join",
    "asymmetric-repartition-join",
]

[categories.distributed.partial-aggregation]
description = "Two/three-phase aggregation for distributed execution"
rules = [
    "two-phase-aggregation",
    "three-phase-distinct-agg",
    "push-partial-agg-below-exchange",
]

[categories.distributed.partition-pruning]
description = "Eliminate irrelevant partitions from distributed scans"
rules = [
    "static-partition-pruning",
    "dynamic-partition-pruning",
    "partition-wise-join",
]

[categories.distributed.distributed-sort]
description = "Distributed sorting and top-N strategies"
rules = [
    "merge-sort-gather",
    "distributed-topn",
    "distributed-window-function",
]

[categories.distributed.colocation]
description = "Data co-location and exchange elimination strategies"
rules = [
    "reference-table-colocation",
    "partition-key-colocation",
    "reference-table-join",
    "colocation-aware-placement",
    "multi-join-colocation",
]

[categories.distributed.stage-planning]
description = "Query stage decomposition and pipeline planning"
rules = [
    "stage-decomposition",
    "pipeline-stages",
    "minimize-stage-count",
]

[categories.multi-model]
description = "Multi-model optimization rules for graph, document, and time-series databases"

[categories.multi-model.graph]
description = "Graph database traversal and pattern matching optimizations"
rules = [
    "join-to-traversal",
    "bidirectional-search",
    "path-materialization",
    "vertex-centric-index",
    "expand-into",
    "label-scan-pushdown",
    "pattern-decomposition",
    "degree-aware-join-ordering",
    "predicate-pushdown-through-traversal",
    "subgraph-isomorphism-pruning",
]

[categories.multi-model.document]
description = "Document database query and pipeline optimizations"
rules = [
    "nested-predicate-pushdown",
    "projection-to-covered-query",
    "array-unwind-pushdown",
    "schema-inference-pushdown",
    "lookup-to-embedded",
    "compound-index-selection",
    "pipeline-coalescence",
    "shard-key-targeted-query",
    "group-push-accumulator",
    "change-stream-filter-pushdown",
]

[categories.multi-model.timeseries]
description = "Time-series database scan, aggregation, and pruning optimizations"
rules = [
    "time-range-pruning",
    "downsampling-pushdown",
    "last-point-optimization",
    "gap-fill-pushdown",
    "retention-policy-pruning",
    "window-function-pushdown",
    "segment-merge-elimination",
    "tag-index-scan",
    "aligned-aggregation-merge",
    "delta-encoding-scan",
]

[categories.experimental]
description = "Research and experimental rules"
rules = []
