# LaTeX Math Rendering Test

This page tests LaTeX/MathJax rendering.

## Inline Math

Selection operator: $\sigma_{p}(R)$

Projection: $\pi_{A}(R)$

Join: $R \bowtie S$

## Block Math

$$
\sigma_{p}(R) = \{t \in R \mid p(t)\}
$$

## Complex Formula

$$
\text{Cost}(Join) = |R| + |S| + |R \bowtie S|
$$
