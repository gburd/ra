# LaTeX Math Test

This tests inline and block LaTeX rendering.

## Inline Math

Here is an inline equation: $\sigma_{p}(R)$ which represents selection.

## Block Math

Here is a block equation:

$$
\sigma_{p}(R) = \{t \in R \mid p(t)\}
$$

## Multiple Equations

Projection: $\pi_{A_1, A_2, ..., A_n}(R)$

Join: $R \bowtie_{p} S$

Union: $R \cup S$

## Complex Block Math

$$
\begin{align}
\text{Cost}(Plan) &= \sum_{i=1}^{n} \text{Cost}(Op_i) \\
&= C_{scan} + C_{join} + C_{project}
\end{align}
$$
