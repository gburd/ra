# Web UI

The web UI (`ra-web`) is not currently part of the RA workspace. Use the CLI (`ra-cli`) for query optimization and plan exploration.

See [Getting Started](../getting-started.md) for CLI usage examples.
